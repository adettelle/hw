apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullname" . }}-sender
spec:
  type: ClusterIP
  selector:
    app: {{ include "fullname" . }}-sender
  ports:
    - port: 8080
      targetPort: 8080
      name: http
